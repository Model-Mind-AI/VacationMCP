# Environment Configuration

Set the following environment variables for local development and deployment.

- API_KEY: Secret key required for all API requests via header `X-API-Key`

Examples (PowerShell):

```powershell
$env:API_KEY = "devkey"
```

Examples (bash):

```bash
export API_KEY=devkey
```
